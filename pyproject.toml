[project]
name = "rbrp_dup"
version = "0.1.0"
description = "Q-learning proof-of-concept for the restricted block relocation problem with duplicate priorities"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
